---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: zrokenvironments.zrok.k8s.zrok.io
spec:
  group: zrok.k8s.zrok.io
  names:
    kind: ZrokEnvironment
    listKind: ZrokEnvironmentList
    plural: zrokenvironments
    shortNames:
    - zrokkenv
    singular: zrokenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.agentReady
      name: Agent
      type: boolean
    - jsonPath: .status.envZID
      name: EnvZID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZrokEnvironment is the Schema for the zrokenvironments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZrokEnvironmentSpec defines the desired state of ZrokEnvironment.
            properties:
              agent:
                description: Agent configures the per-environment zrok2 agent Deployment.
                properties:
                  consolePort:
                    default: 8888
                    description: ConsolePort is the agent HTTP console / gRPC-gateway
                      port.
                    format: int32
                    type: integer
                  image:
                    description: Image is the container image for the agent. Defaults
                      to openziti/zrok2 pinned version.
                    type: string
                  persistence:
                    description: Persistence configures the PVC that stores ~/.zrok2
                      state.
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size of the PVC. Defaults to 1Gi.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName for the PVC. Uses cluster default
                          when unset.
                        type: string
                    type: object
                  replicas:
                    default: 1
                    description: Replicas is the desired agent replica count. Must
                      be 1 until agent HA is supported.
                    format: int32
                    maximum: 1
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources for the agent container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              apiEndpoint:
                description: |-
                  ApiEndpoint is the zrok controller API URL.
                  Defaults to https://api-v2.zrok.io when empty. Set for self-hosted instances.
                type: string
              enableTokenSecretRef:
                description: EnableTokenSecretRef references a Secret key containing
                  the zrok account enable token.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              reclaimPolicy:
                default: Delete
                description: ReclaimPolicy controls whether the remote zrok environment
                  is disabled on delete.
                enum:
                - Delete
                - Retain
                type: string
              uniqueID:
                description: "UniqueID prefixes the remote zrok environment host and
                  description\n(`{uniqueID}/zrok-operator/{namespace}/{name}`). When
                  empty, defaults to\nthe UUID (metadata.uid) of the kube-system Namespace:\n\n\tkubectl
                  get ns kube-system -o jsonpath='{.metadata.uid}'\n\nApplied at Enable;
                  changing this later does not rename an existing remote env."
                type: string
            required:
            - enableTokenSecretRef
            type: object
          status:
            description: ZrokEnvironmentStatus defines the observed state of ZrokEnvironment.
            properties:
              agentReady:
                description: AgentReady reports whether the agent Deployment is available.
                type: boolean
              agentService:
                description: AgentService is the cluster DNS name of the agent Service.
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              envZID:
                description: EnvZID is the Ziti identity of the enabled zrok environment.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              uniqueID:
                description: UniqueID is the prefix used at Enable (spec.uniqueID,
                  or kube-system Namespace UUID).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
