---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: zrokshares.zrok.k8s.zrok.io
spec:
  group: zrok.k8s.zrok.io
  names:
    kind: ZrokShare
    listKind: ZrokShareList
    plural: zrokshares
    shortNames:
    - zrokshare
    singular: zrokshare
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.assignedURL
      name: URL
      type: string
    - jsonPath: .status.shareToken
      name: Token
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZrokShare is the Schema for the zrokshares API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZrokShareSpec defines the desired state of ZrokShare.
            properties:
              accessGrants:
                description: AccessGrants lists zrok account emails allowed to access
                  a closed share.
                items:
                  type: string
                type: array
              backendMode:
                allOf:
                - enum:
                  - proxy
                  - web
                  - caddy
                  - drive
                  - tcpTunnel
                  - udpTunnel
                  - socks
                - enum:
                  - proxy
                  - web
                  - caddy
                  - drive
                  - tcpTunnel
                  - udpTunnel
                  - socks
                default: proxy
                description: BackendMode selects the zrok backend. Default proxy.
                type: string
              basicAuthSecretRef:
                description: BasicAuthSecretRef references a Secret with keys "username"
                  and "password".
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              closed:
                description: Closed enables closed permission mode (requires AccessGrants).
                type: boolean
              environmentRef:
                description: EnvironmentRef references a ZrokEnvironment in the same
                  namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              insecure:
                description: Insecure skips TLS verification when dialing the upstream
                  target.
                type: boolean
              nameSelection:
                description: |-
                  NameSelection optionally binds a reserved namespace name for a sticky frontend URL.
                  Strongly recommended so the agent restarts the share after pod restarts.
                properties:
                  name:
                    description: Name is the reserved frontend name within the namespace.
                    minLength: 1
                    type: string
                  namespace:
                    default: public
                    description: Namespace is the namespace token (typically "public").
                    type: string
                required:
                - name
                type: object
              oauth:
                description: OAuth configures OAuth front-door protection for public
                  shares.
                properties:
                  emailDomains:
                    description: EmailDomains restricts access to matching email domain
                      globs.
                    items:
                      type: string
                    type: array
                  provider:
                    description: Provider is google or github.
                    enum:
                    - google
                    - github
                    type: string
                  refreshInterval:
                    description: RefreshInterval is the OAuth session lifetime (Go
                      duration string).
                    type: string
                required:
                - provider
                type: object
              privateShareToken:
                description: PrivateShareToken sets a vanity token for private shares.
                type: string
              reclaimPolicy:
                default: Delete
                description: ReclaimPolicy controls whether reserved names are deleted
                  on CR delete.
                enum:
                - Delete
                - Retain
                type: string
              shareMode:
                allOf:
                - enum:
                  - public
                  - private
                - enum:
                  - public
                  - private
                default: public
                description: |-
                  ShareMode is public or private. MVP implements public; private is accepted in schema
                  and rejected with a clear condition until fully implemented.
                type: string
              upstream:
                description: Upstream is the in-cluster target to share.
                properties:
                  url:
                    description: URL is a full upstream URL, e.g. http://mysvc.default.svc:80
                    minLength: 1
                    pattern: ^(https?|tcp|udp)://.+
                    type: string
                required:
                - url
                type: object
            required:
            - environmentRef
            - upstream
            type: object
          status:
            description: ZrokShareStatus defines the observed state of ZrokShare.
            properties:
              assignedURL:
                description: AssignedURL is the primary public frontend URL.
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              frontendEndpoints:
                description: FrontendEndpoints lists all frontend proxy endpoints
                  for the share.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              shareToken:
                description: ShareToken is the zrok share token.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
